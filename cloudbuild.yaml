steps:
    - name: 'gcr.io/cloud-builders/gcloud'
      args: ['run', 'deploy', 'endpoints-runtime-serverless', '--image', 'gcr.io/$PROJECT_ID/endpoints-runtime-serverless', '--region', 'europe-west1', '--platform', 'managed', '--allow-unauthenticated']

images:
    - 'gcr.io/$PROJECT_ID/endpoints-runtime-serverless'